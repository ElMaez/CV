---
import "../pages/css/animation.css";
---

<header>
  <nav>
    <ul>
      <li class="menuLi"><a class="menuBtn" href="#">Home</a></li>
      <li class="menuLi">
        <a class="menuBtn" href="#qualifications">Qualifications</a>
        <div class="test"></div>
      </li>
      <li class="menuLi"><a class="menuBtn" href="#about">About Me</a></li>
      <li class="menuLi"><a class="menuBtn" href="#education">Education</a></li>
      <li class="menuLi">
        <a class="menuBtn" href="#experience">Experience</a>
      </li>
    </ul>
  </nav>
</header>

<style>
  header {
    background-image: linear-gradient(
        260deg,
        rgb(0, 0, 0) 0%,
        rgba(0, 0, 0, 0.4) 35%,
        rgba(0, 0, 0, 0) 100%
      ),
      url(/src/assets/img/DSC08462Flip.png);
    background-repeat: no-repeat;
    background-position: 30% 18%;

    border-radius: 1rem;
    border: solid 0.4rem rgb(15, 15, 15);

    height: 30rem;
  }
  nav {
    height: 100%;
    padding: 5rem 2rem;
  }
  nav ul {
    display: grid;
    grid-template-columns: 1fr 1fr;
    grid-template-rows: 1fr;
    row-gap: 0.5rem;
  }
  nav ul li {
    grid-column: 2/-1;

    display: flex;
    flex-direction: column;
    justify-self: flex-end;
    overflow: hidden;
    height: 2.5rem;
  }

  a {
    position: relative;
    text-align: end;

    &::before {
      content: "";
      position: absolute;
      inset: auto 0 0;
      height: 2px;
      background: var(--colorMenu);
      scale: 0 1;
      transform-origin: left;
      transition: scale 0.8s linear;
    }

    &:hover::before {
      scale: 1 1;
      transform-origin: right;
    }
  }
</style>
